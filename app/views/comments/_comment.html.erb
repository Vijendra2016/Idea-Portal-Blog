<%= form_for :comment,:html => { :id => 'comment' } do |f| %>
  <p>
    <%= f.label :commenter %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
  <!--   
     <%= f.submit %>
   -->
   <input type="button" id="btn-comment-save" value="Add Comment"></input>
  </p>
<% end %>